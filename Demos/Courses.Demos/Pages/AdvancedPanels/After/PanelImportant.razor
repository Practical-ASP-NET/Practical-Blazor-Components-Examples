<Panel Title="Title" ChildContent="ChildContent" TitleClass="bg-danger" Dismissable="false"/>

@code {
    
    [Parameter]
    public string Title { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
    
}